## Google Workspace 利用ルール

- **Gmail**: `gws` CLI を使うこと（メール検索・取得・既読化など）
- **Google Calendar**: MCP ツール (`mcp__claude_ai_Google_Calendar__gcal_list_events` 等) を使うこと。`gws calendar` は使わない
- **Google Drive**: MCP ツール (`mcp__claude_ai_Google_Drive__*`) を使うこと
